
version: "3.9"

services:
  app:
    image: golang:1.22-alpine
    container_name: chat-app-dev
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "8080:8080"
    command: go run main.go
